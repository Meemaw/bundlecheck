{
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "clean": "lerna run --stream --concurrency 1 clean",
    "lint": "lerna run --stream --concurrency 1 lint",
    "test": "lerna run --stream --concurrency 1 --sort test",
    "test:coverage": "lerna run --stream --concurrency 1 --sort test:coverage"
  },
  "devDependencies": {
    "lerna": "^3.18.4"
  }
}
